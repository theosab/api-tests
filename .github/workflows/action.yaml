name: Test Login Endpoint
on: [push]
jobs:
  dev-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '1v20.13.1
'
      - run: npm install
      - run: npm test